import {
  getPosts,
  createPost,
  deletePos,
  updatePost,
} from './post';

import {
  signUp,
  signIn,
} from './user';

export default {
  getPosts,
  createPost,
  deletePos,
  updatePost,
  signUp,
  signIn,
};
